import React from 'react';
import Search from '../search'

const Sidebar = () => {
    return(
        <div className="sidebar">
            <Search />
        </div>
    );
}

export default Sidebar;